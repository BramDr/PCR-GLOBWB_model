name: Code formatting

on: [pull_request]

jobs:
  black:
    # Check if the pull-request is not raised by this workflow
    if: startsWith(github.head_ref, 'black-patches') == false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Black
        id: black
        uses: psf/black@stable
        with:
          args: "."
        continue-on-error: true
      - name: Run Create Pull Request
        if: steps.black.outputs.exit-code > 0
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: black patches
          title: Fixes by black
          body: This is an auto-generated pull-request with patches by black.
          labels: black
          branch: black-patches/${{ github.head_ref }}
      - name: Fail if black made changes
        if: steps.black.outputs.exit-code > 0
        run: exit 1